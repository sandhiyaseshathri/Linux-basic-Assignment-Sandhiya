# Linux-basic-Assignment-Sandhiya
Answers
                    **SECTION 1**
pwd -->To see te current directory name where we are working.
ls -->To see all the list or files in the directory.
cd -->To change the directory.
mkdir --> It is make directory. to create new directory.
rm-rf -->This command is used to remove file from the directory.
ps-ef --> To see what are the processors run n the local machine.
top -->To list out the high CPU comsuming applications.
df-h --> To check the storage space.
history -->To display what are the coommands are executed from the begining.
uptime -->Shows how long the system has been running.

                   **SECTION 2**

mkdir project-files
cd project-files
vi notes.txt
cat notes.txt
CP notes.txt backup.txt
head-100 logs.txt
tail-100 logs.txt
df-h
ps-ef
rm temp.txt

                     **SECTION 3**
> ......Used to overwrite the content on the file and >>....used to append the content(at the last) on the file.
> Kill-9 command is used to kill the application the system. and forcefully termibates the process using its processor ID
> rm is  used to remove the file and the directory...and the rmdir is used to remove the empty directory in the system.
> netstat-tupln command provide the information about to see what are the applications run on the port of the system.
> ping command is used to know  that "if our server requests the another server(communicate with each other).
> and also check the connectivity between the 2 systems.

                       **SECTION 4**
pwd
mkdir/devops
top
ping google.com
tail -50 application.log




                       
